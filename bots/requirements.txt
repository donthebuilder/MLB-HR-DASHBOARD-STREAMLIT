# Bot-side deps. Installed by GitHub Actions only, never by Streamlit Cloud.
requests>=2.31.0
pandas>=2.0.0
pybaseball>=2.2.0
reportlab>=4.0.0
pillow>=10.0.0

# DASH social pipeline (2026-08-21) — bots/social/captions.py only. Talks to
# the Claude API to write captions; never touches model/grading logic.
anthropic>=0.40.0
